# @caoji/config

Shared configuration package for the workspace.

## 📦 Contents

- **ESLint**: Shared ESLint configuration (`eslint.config.js`)
- **Prettier**: Shared Prettier configuration (`prettier.config.js`)
- **TypeScript**: Shared TypeScript configuration (`tsconfig.json`)

## 🚀 Usage

Import these configurations in other packages/apps to maintain consistency.
